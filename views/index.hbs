<div class="container-fluid">
    <div class="row">
{{#each products}}
        <a href="productDetails/{{@index}}">
            <div class="col-md-8 mx-auto text-center border mt-4 p-3 bg-light">
                <h4>{{title}}</h4>
                <p>{{description}}</p>
            </div>
        </a>

{{/each}}

    </div>
</div>

